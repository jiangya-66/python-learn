# 信息抽取器依赖
# 使用 pip install -r requirements.txt 安装

# 核心依赖
pydantic>=2.0.0          # 数据验证和序列化
openai>=1.0.0            # DeepSeek API客户端（兼容OpenAI格式）

# 可选依赖（用于更高级的用法）
python-dotenv>=1.0.0     # 环境变量管理
httpx>=0.25.0            # HTTP客户端（openai依赖）
anyio>=4.0.0             # 异步I/O（openai依赖）

# 开发依赖
pytest>=7.0.0            # 测试框架
black>=23.0.0            # 代码格式化
mypy>=1.0.0              # 类型检查